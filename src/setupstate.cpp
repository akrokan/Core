#include "include/setupstate.h"

SetupState::SetupState()
{
// A hand of cards
//    std::vector<std::shared_ptr<Entity>> Hand;

// Creating a test card
//    Entity Card;

// Assigning a name to it
//    NameComponent* name = new NameComponent;
//    name->setName("Summons");
//    Card.addComponent(std::type_index(typeid(NameComponent)), name);

// Pushing it in the hand
//    Hand.push_back(std::make_shared<Entity>(Card));

// Showing stuff if the code work (debug)
//    std::cout << "C " << Card.get<NameComponent>()->getName() << std::endl;
//    std::cout << "H " << Hand[0]->get<NameComponent>()->getName() << std::endl;
//    std::cout << std::endl;
}

